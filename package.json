{
  "name": "tsla-ble-dash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "test": "tsc --project tsconfig.test.json && node scripts/fix-test-build.mjs && node --experimental-specifier-resolution=node --test test/*.mjs"
  },
  "dependencies": {
    "protobufjs": "^7.2.4",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.17",
    "protobufjs-cli": "^1.1.3",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
